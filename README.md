#### Active Vision Experiments
  
To install you need the uv python package / virtual env manager.
And ffmpeg on your development environment PATH.

See all options with `python src/auga/cli.py run --help`
